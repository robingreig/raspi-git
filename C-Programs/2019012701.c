// comment: Adding 2 numbers together
#include <stdio.h>

void main (void)
{
    int a;
    int b = 3.641;
    int c;
    a = 2;
    c = a + b; // This is also a comment & ignored
    printf("the sum of adding %d and %d is %d\n",a,b,c);
}
