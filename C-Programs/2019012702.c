// comment: Adding 2 numbers together
#include <stdio.h>

void main (void)
{
    int a;
    float b = 3.641;
    float c;
    a = 2;
    c = a + b; // This is also a comment & ignored
    printf("the sum of adding %d and %f is %.2f\n",a,b,c);
}
