// comment: Adding 2 numbers together
#include <stdio.h>

void main (void)
{
    int a = 1;
    
    switch (a)
    {
	    case 0 : printf("a is equal to %d\n", a);
				 break;
		case 1 : printf("a is eaual to %d\n", a);
				 break;
		default : printf ("a is greater than 1\n");
    }
}
