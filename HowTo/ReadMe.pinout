
gpio readall 
- Will display status of all gpio pins

pinout
- Will display a graphical representation of the raspi with version #, etc.

HDMI on & off
sudo /opt/vc/bin/tvservice -o (or -p to turn it back on)


