^A to access most commands

^A " = Show window list

^A A = Name current window

^A c = Create & switch to a new window with a shell

^A D = Detach & logout

^A \ = Kill all windows and terminate screen

screen -x to reattach screen session to a new terminal

screen /dev/serial0 9600 = to talk to Gertduino via serial port
